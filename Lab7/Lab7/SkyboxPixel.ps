TextureCube skyboxTexture : register(t0);

SamplerState sam: register(s0);

struct PS_INPUT
{
    float4 position : SV_POSITION;
    float3 tex: TEXCOORD;
};

float4 main(PS_INPUT input) : SV_Target
{
    return skyboxTexture.Sample(sam, input.tex);
}